# Install CPU-only PyTorch wheels. On Linux (e.g. Render) the default PyPI
# `torch` is the multi-GB CUDA build, which bloats the image and inflates
# memory use; the CPU wheels below are far smaller and all this app needs.
--extra-index-url https://download.pytorch.org/whl/cpu

Flask
Pillow

# Production WSGI server (Linux/Unix). Run with:
#   gunicorn -c gunicorn.conf.py app:app
# On Windows, gunicorn does not run; use `python app.py` or `waitress`.
gunicorn

# Inference dependencies for the TESSNet ResNet18 model (CPU build).
torch==2.12.1+cpu
torchvision==0.27.1+cpu

# Full light-curve analysis pipeline (CSV + TIC ID input modes).
# Optional: the image-upload mode works without these.
# matplotlib runs fully headless here — the app forces the non-GUI "Agg"
# backend (see pipeline.py), so no display/GUI toolkit is required.
lightkurve
matplotlib
